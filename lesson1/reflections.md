# Reflections on assignment 1

## Exercise 1b
The system prompt had more effect then the temperature affects.
The system prompt actively dictates the rules of the LLM. Overriding everything.

## Exercise 1c
Forcing a quote forces the model to provided context rather than relying on its training data.
Allowing an "I don't know" removes the models habit to please the user by hallucinate.

## Exercise 2
Using a closed API model for access to state-of-the-art frontier capabilities, but sacrifices privacy and cost.
Running an open model locally is totaly private, offline, and avoids per-token costs.



